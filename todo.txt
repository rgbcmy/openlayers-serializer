      projection的默认值 应该不需要设置为'EPSG:3857',因为它默认会用view里面的坐标
      //todo 从urls推断出url
      //source 
      //BingMaps  目前已经失效了好像待确认
      Google //需要token
      //以下这几个是XYZ的子类
      CartoDB //这个已经失效了，可去ol仓库提pr
      //函数的序列化和反序列化比较重要，主要是要内置一些常用的函数
      1.各种Style要注入
      2.基础类要加入，如果Feature这些
      3.编写的时候需要有智能感知 推荐采用monaco-editor（vscode的面板）

      npm publish --access public   为发布到npm上